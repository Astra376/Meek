package com.example.aichat.core.util

import androidx.compose.ui.graphics.Color
import kotlin.math.absoluteValue

fun avatarPalette(seed: String): List<Color> {
    return listOf(Color(0xFF2D2D2D), Color(0xFF111111))
}
